name: Test CI/CD

on:
  pull_request:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:

      - name: Install Aqua
        uses: aquaproj/aqua-installer@v4.0.4
        with:
          aqua_version: v2.48.1
